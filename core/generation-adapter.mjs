// Provider-neutral generation boundary. Canonical state never stores provider request IDs.
export function createGenerationRequest({project,pageIndex=0,cleanAsset,prompt,referenceAssets=[]}){const page=project.pages[pageIndex];if(!page)throw new Error('Unknown page');return{schema:'manga-generation-request/1',pageId:page.id,authority:{spatial:'clean',semantic:'project',identity:'references'},inputs:{cleanAsset,prompt,references:referenceAssets.map(r=>({referenceKey:r.referenceKey,asset:r.asset,role:r.role||'identity'}))},constraints:{preservePanelGeometry:true,preserveReadingDirection:project.meta.readingDirection,preserveVisibleText:true}}}
export function createGenerationResult(request,{provider='external',model=null,outputAsset=null,providerRequestId=null,metadata={}}={}){return{schema:'manga-generation-result/1',requestSchema:request.schema,provider,model,outputAsset,provenance:{providerRequestId,createdAt:new Date().toISOString(),metadata}}}
export function validateReferenceBindings(project,assets=[]){const keys=new Set(assets.map(a=>a.referenceKey)),required=[...new Set((project.characterLibrary||[]).filter(c=>c.identityMode==='sheet'||c.referenceKey).map(c=>c.referenceKey).filter(Boolean))];return{required,missing:required.filter(k=>!keys.has(k)),bound:required.filter(k=>keys.has(k))}}
export function buildPortableGenerationPackage({project,pageIndex=0,cleanAsset,prompt,referenceAssets=[]}){const bindings=validateReferenceBindings(project,referenceAssets),request=createGenerationRequest({project,pageIndex,cleanAsset,prompt,referenceAssets});return{schema:'manga-generation-package/1',request,bindings,ready:bindings.missing.length===0,providerAdapter:{mode:'external-boundary',canonicalStateProviderIndependent:true}}}
